terraform {
  backend "s3" {
    bucket         = "my-terraform-states"
    key            = "dev/us-east-1/vpc.tfstate"
    region         = "us-east-1"
    dynamodb_table = "terraform-locks"
    encrypt        = true
  }
}